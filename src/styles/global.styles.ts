import {StyleSheet} from 'react-native';

export const fontSize = {
  h1: {
    fontSize: 20,
  },

  h2: {
    fontSize: 16,
  },

  h3: {
    fontSize: 14,
  },

  h4: {
    fontSize: 12,
  },
};

export const globalStyles = StyleSheet.create({
  flex: {
    flex: 1,
  },
});
