import {StyleSheet} from 'react-native';
import {color} from '../styles/color.style';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: color.darkJade,
  },
});

export default styles;
